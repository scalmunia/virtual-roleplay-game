// import { collection, query, where } from "firebase/firestore";
// import { useFirestore } from "reactfire";

// // export function useDoc(path: string, ...queryConstraints: QueryConstraint[], id: string) {
// export function useDoc(path: string) {
//   const firestore = useFirestore();
//   const ref = collection(firestore, path) as CollectionReference<D>;
//   // const q = query<D>(ref, ...queryConstraints);


// }